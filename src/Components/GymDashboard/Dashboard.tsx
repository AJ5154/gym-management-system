import { Button } from "@mui/material";

const Dashboard = () => {
  return (
    <div>
      <Button variant="contained" href="/">
        Logout
      </Button>
    </div>
  );
};

export default Dashboard;
